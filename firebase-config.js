// Your web app's Firebase configuration, from the Firebase console
// (Project settings > General > Your apps > SDK setup and configuration).
//
// This config is safe to be public in a static site; access to your data is
// controlled by Firestore security rules (see firestore.rules), not by keeping
// this file secret.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
