{
  "name": "family-garage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A vehicle log for a family's cars: fuel, MPG, and service tracking. Static HTML/CSS/JS, no build step, backed by Firebase Firestore.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node tests/run.mjs",
    "test:unit": "node tests/run.mjs --unit-only",
    "test:e2e": "node tests/run.mjs --e2e-only"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
